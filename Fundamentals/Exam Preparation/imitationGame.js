function imitationGame (input){

    let encryptedMessage = input.shift()

    let index = 0
    let line = input[index]
    index++

    while(line !== "Decode"){

        let line = input[index]
        index++

    }

    while

}

imitationGame([

    'zzHe',
    'ChangeAll|z|l',
    'Insert|2|o',
    'Move|3', 
    'Decode'
   
    ])
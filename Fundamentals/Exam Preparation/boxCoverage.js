function boxGoc (input){

    let sizeOfSide = input.shift()
    let numberOfSheets = input.shift()

    let squareArea = sizeOfSide * sizeOfSide* 6
    

    for(let i = 0; i < input.length; i++){

        
    }
}


boxGoc ([2,
    2,
    0.2,
    7,
    6,
    8.5
    ])
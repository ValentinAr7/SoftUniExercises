function oreders(drink, quantity){

    let coffee = 1.50
    let water = 1.00
    let coke = 1.40
    let snacks = 2.00

    let totalPrice = 0;

    switch (totalPrice) {
        case drink = coffee: totalPrice = drink * quantity 
            break;
    
        default:
            break;
    }
console.log(totalPrice);

}

oreders("coffee", 5)
function houseParty(arr){

    let newGuestList = []

    for (const el of arr) {
        let command = el.split(" ")
        let name = command[0]
        console.log(command);

        if(el.length === 3){


        }
    }


}
houseParty(['Allie is going!',
            'George is going!',
            'John is not going!',
            'George is not going!'])